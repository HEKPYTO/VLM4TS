services:
  app:
    build: .
    ports:
      - "8501:8501"
    environment:
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}

  bench:
    build: .
    command: python scripts/bench.py
    profiles: ["bench"]
